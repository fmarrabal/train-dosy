# Manuscript v4 / Manuscrito v4

The mathematical revision separately formulates TRAIn, native atomic RAI, density RAI, adaptive Haar RAI, original and revised RAI-Net, DOME, RAI-S/DOME-S and TRAIn-MF. The appendix adds the precise Haar gap, neural features, DOME averaging, RAI-FLEX, partial-C and CIRCE reconstruction adapters. Each description distinguishes its objective, representation, constraints, numerical updates, regularization, selection target and limits. A 27-file SHA-256 source/equation manifest supports the transcription.

Algorithm 1 is a real algorithm float using `algorithm` and `algpseudocode`, with numbered lines, declared inputs/outputs, candidate loops, numerical eligibility, paired predictive selection and final refitting. Appendix equation hyperlinks are disambiguated from main-text equations.

This revision changes the manuscript and bilingual mathematical documentation. It makes no changes to the numerical engines, benchmark inputs, saved estimates, metrics, scientific figures or 48 references. Numerical software stays v0.1.0. Historical variants are explicitly scoped: this release does not add full neural retraining or the complete historical survey to the public API. Author declarations remain pending. GitHub Actions remains disabled; PDF compilation and verification run locally.

La revisión desarrolla por separado los objetivos, variables, restricciones, actualizaciones, regularización, selección y límites de cada método. El Algoritmo 1 pasa a un entorno de algoritmo real con pseudocódigo numerado. Se documentan las diferencias entre TRAIn, la versión histórica MFV31 y la referencia TRAIn-MF actual; entre RAI atómico, de densidad y Haar; y entre las dos políticas RAI-Net. El apéndice incluye RAI-FLEX, partial-C, CIRCE y promedio DOME.

No se recalculan modelos ni se modifican resultados, figuras, núcleos numéricos o referencias. El mapa de ecuaciones identifica 27 archivos por SHA-256. Las declaraciones de autoría siguen pendientes; las variantes históricas descritas no se presentan como nuevos métodos ejecutables de la API.

Validation: 36 PDF pages visually reviewed; no overfull boxes, undefined references or duplicate equation destinations; 20 deterministic algebra checks passed. SHA-256 verification confirms 22 preserved source files, 27 formulation/source records, 82 unchanged numerical files relative to v0.1.0, all eight current scientific figure files and all 48 references.
