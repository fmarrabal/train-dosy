# Manuscript v5 / Manuscrito v5

This revision incorporates Victor Valdivieso as first author, Ignacio Fernández as second author, and Francisco Manuel Arrabal-Campos as corresponding author, with the supplied affiliations, ORCIDs, emails and funding. Acknowledgments are removed. CRediT roles are explicitly proposed for confirmation by all authors; conflict declarations remain pending. The AI-use statement is retained in Methods, with its journal-specific placement documented as a submission issue.

The mathematical audit distinguishes density-RAI exact uniqueness from numerical termination; specifies RAI scaling and noise-standardized validation; expresses DOME proposals in their actual dimensionless coordinates; and makes Algorithm 1's diagnostic log precise. A new appendix proves a sufficient condition for the conditional TRAIn-MF profile update to have a unique minimizer, constructs a curvature-null degeneracy example, and derives screening and paired-loss identities under specified covariance. These are fixed-design theoretical results, not newly validated correlated-noise algorithms.

The abstract now reports DOME's order-only regression and increased unmatched mass alongside the improved spectral assignment and prediction. RAI-Net is described as a multi-factor policy comparison, not an isolated ablation; a nonsignificant change is not treated as evidence of equivalence. A clipped color label in the discrete DOSY illustration is repaired in the manuscript layout without altering any source figure or plotted data.

Validation: 38 PDF pages reviewed; no LaTeX overfull boxes, undefined references or duplicate destinations; 27 deterministic algebra checks passed. An independent saved-data audit reproduces the primary means and six paired bootstrap intervals, retains both archived neural failures, verifies 22 preserved sources and 27 source/equation identities, and checks the author metadata. All 82 numerical files compared with v0.1.0, all eight figure files, the frozen metrics and 48 references are unchanged. The 46 DOI records are compared with the dated 25 September 2026 cache, not represented as a fresh network verification. Numerical software remains v0.1.0; no models are refitted. GitHub Actions remains disabled.

La revisión incorpora el orden de firma, afiliaciones, ORCID, correspondencia y financiación facilitados. CRediT se presenta como propuesta pendiente de ratificación y los conflictos quedan sin inventar. Se eliminan los agradecimientos y se conserva la declaración de uso de IA en metodología, con su ubicación editorial pendiente documentada.

La auditoría corrige la interpretación de convergencia en RAI de densidad, explicita el escalado y los criterios de validación, formula DOME en coordenadas adimensionales y precisa el registro del Algoritmo 1. Añade una condición suficiente de unicidad condicional para S en TRAIn-MF, un contraejemplo de degeneración y las identidades con covarianza especificada. El resumen expone las regresiones de orden y masa no emparejada; la comparación neuronal deja de presentarse como una ablación aislada.

Se revisan las 38 páginas, se superan 27 comprobaciones algebraicas y se reproducen agregaciones e intervalos desde resultados guardados. No cambian los estimadores, datos, resultados, figuras originales ni referencias. El informe detallado en español está en `paper/AUDITORIA_MANUSCRITO_ES.md`; la auditoría reproducible se ejecuta con `python paper/scripts/audit_manuscript.py`.
